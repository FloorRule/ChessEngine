#pragma once
#include "Point.h"
#include <iostream>
/*

using std::string;

class Board
{
private:
	int _maxElements;
	string _board[8];

public:
	Board(int maxElements, string game[]);
	~Board();

	void wordsToNums(string& point);
	string* get_board();
	void set_board(string board);
	bool is_there_tool(Point nextPos);
	void updateBoard(Point lastPlace, Point movedPoint, string mark);
	bool is_there_tool_rook(Rook rook, Point pos, Point nextPos);

};
*/